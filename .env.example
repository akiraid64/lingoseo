# Generate with: openssl rand -base64 32
AUTH_SECRET=

# GitHub OAuth App (github.com > Settings > Developer Settings > OAuth Apps)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Lingo.dev API key (for server-side translations via SDK)
LINGO_API_KEY=
